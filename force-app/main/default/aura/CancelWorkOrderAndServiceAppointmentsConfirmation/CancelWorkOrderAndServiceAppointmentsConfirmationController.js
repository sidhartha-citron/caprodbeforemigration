({
	confirmCancel : function(component, event, helper) {
        helper.handleConfirmCancel(component, event, helper);
	},
    
    cancel : function(component, event, helper) {
		helper.handleCancel(component, event, helper);
	}
})