({
    firstPage: function(component, event, helper) {
        helper.handleFirstPage(component, event, helper);
    },
    
    prevPage: function(component, event, helper) {
        helper.handlePrevPage(component, event, helper);
    },
    
    nextPage: function(component, event, helper) {
        helper.handleNextPage(component, event, helper);
    }
})