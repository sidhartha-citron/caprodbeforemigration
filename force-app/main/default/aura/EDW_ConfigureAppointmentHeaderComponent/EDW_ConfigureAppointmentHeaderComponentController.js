({
    cancelOrder: function(component, event, helper) {
        helper.handleCancelOrder(component, event, helper);
    },
    
    closeWizard: function(component, event, helper) {
        helper.handleCloseWizard(component, event, helper);
    }
})