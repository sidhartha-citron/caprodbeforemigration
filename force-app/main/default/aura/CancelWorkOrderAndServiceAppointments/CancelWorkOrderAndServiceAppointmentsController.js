({
	cancelWorkOrder: function(component, event, helper) {
        helper.handleCancelWorkOrder(component, event, helper);
	},
    
    cancellationConfirmed: function(component, event, helper) {
        helper.handleCancellationConfirmed(component, event, helper);
    }
})